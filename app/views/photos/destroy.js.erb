$('#photos').html("<%= j render 'photos_list' %>")
<% if @claim.attachments.blank? %>
  $('#photo_check').hide();
<% end %>

<% if !@claim.active && !@claim.price.blank? && !@claim.listing_name.blank? && !@claim.attachments.blank? && !@claim.address.blank? %>
    $('#publish_button').attr('disabled', false);
<% else %>
    $('#publish_button').attr('disabled', true);
<% end %>
